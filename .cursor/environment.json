{
  "name": "harness template",
  "install": "test -f package-lock.json && npm ci || (test -f requirements.txt && pip install -r requirements.txt || echo 'no dependency manifest yet — nothing to install')"
}
